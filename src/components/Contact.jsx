export default function Contact({ mail, phone }) {
    return (
        <div className="contact">
            <h1>CONTACT</h1>
            <p>{mail}</p>
            <p>{phone}</p>
        </div>
    )
}
